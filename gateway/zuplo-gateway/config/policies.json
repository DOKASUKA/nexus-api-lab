{
  "policies": [
    {
      "name": "auth-workers",
      "policyType": "add-request-header",
      "options": {
        "header": "X-Shared-Secret",
        "value": "${env.SHARED_SECRET_AUTH}"
      }
    }
  ]
}