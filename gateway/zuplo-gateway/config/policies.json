{
  "policies": [
    {
      "name": "auth-workers",
      "policyType": "set-headers-outbound", // これが必須！
      "handler": {
        "export": "SetHeadersOutboundPolicy",
        "module": "$import(@zuplo/runtime)",
        "options": {
          "headers": {
            "X-Shared-Secret": "${env.SHARED_SECRET_AUTH}"
          }
        }
      }
    }
  ]
}